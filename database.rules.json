{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('partnerID').val() === $uid || root.child('users').child($uid).child('partnerID').val() === auth.uid)",
        ".write": "auth != null && auth.uid === $uid",
        "sixDigitUID": {
          ".validate": "newData.isString() && newData.val().matches(/^[0-9]{6}$/)"
        },
        "firstName": {
          ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 40"
        },
        "lastName": {
          ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 40"
        },
        "partnerID": {
          ".validate": "(newData.isString() && newData.val() != auth.uid) || !newData.exists()"
        },
        "publicKey": {
          ".validate": "newData.isString() && newData.val().length > 40"
        },
        "moodCiphertext": {
          ".validate": "newData.isString() && newData.val().length > 0"
        },
        "locationCiphertext": {
          ".validate": "newData.isString() && newData.val().length > 0"
        },
        "fcmToken": {
          ".validate": "newData.isString() && newData.val().length > 0"
        }
      }
    },
    "pairCodes": {
      "$code": {
        ".read": "auth != null",
        ".write": "auth != null && (!data.exists() || data.val() === auth.uid) && newData.isString() && newData.val() === auth.uid",
        ".validate": "$code.matches(/^[0-9]{6}$/) && newData.isString() && newData.val() === auth.uid"
      }
    },
    "chats": {
      "$chatId": {
        ".read": "auth != null && root.child('users').child(auth.uid).child('partnerID').val() != null && root.child('users').child(root.child('users').child(auth.uid).child('partnerID').val()).child('partnerID').val() === auth.uid && ($chatId === auth.uid + '_' + root.child('users').child(auth.uid).child('partnerID').val() || $chatId === root.child('users').child(auth.uid).child('partnerID').val() + '_' + auth.uid)",
        ".write": "auth != null && root.child('users').child(auth.uid).child('partnerID').val() != null && root.child('users').child(root.child('users').child(auth.uid).child('partnerID').val()).child('partnerID').val() === auth.uid && ($chatId === auth.uid + '_' + root.child('users').child(auth.uid).child('partnerID').val() || $chatId === root.child('users').child(auth.uid).child('partnerID').val() + '_' + auth.uid)",
        "messages": {
          ".indexOn": ["sentAt"],
          "$messageId": {
            ".validate": "newData.hasChildren(['id','senderID','recipientID','payload','sentAt','keyVersion']) && newData.child('id').val() === $messageId && newData.child('senderID').val() === auth.uid && newData.child('recipientID').val() === root.child('users').child(auth.uid).child('partnerID').val() && newData.child('payload').isString() && newData.child('payload').val().length > 0 && newData.child('sentAt').isNumber() && newData.child('keyVersion').isNumber()"
          }
        }
      }
    },
    "events": {
      "$chatId": {
        ".read": "auth != null && root.child('users').child(auth.uid).child('partnerID').val() != null && root.child('users').child(root.child('users').child(auth.uid).child('partnerID').val()).child('partnerID').val() === auth.uid && ($chatId === auth.uid + '_' + root.child('users').child(auth.uid).child('partnerID').val() || $chatId === root.child('users').child(auth.uid).child('partnerID').val() + '_' + auth.uid)",
        ".write": "auth != null && root.child('users').child(auth.uid).child('partnerID').val() != null && root.child('users').child(root.child('users').child(auth.uid).child('partnerID').val()).child('partnerID').val() === auth.uid && ($chatId === auth.uid + '_' + root.child('users').child(auth.uid).child('partnerID').val() || $chatId === root.child('users').child(auth.uid).child('partnerID').val() + '_' + auth.uid)",
        "heartbeat": {
          "$eventId": {
            ".validate": "newData.hasChildren(['senderID','sentAt']) && newData.child('senderID').val() === auth.uid && newData.child('sentAt').isNumber()"
          }
        }
      }
    }
  }
}
