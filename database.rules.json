{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('partnerID').val() === $uid || root.child('users').child($uid).child('partnerID').val() === auth.uid)",
        ".write": "auth != null && auth.uid === $uid",
        "sixDigitUID": {
          ".validate": "newData.isString() && newData.val().matches(/^[0-9]{6}$/)"
        },
        "firstName": {
          ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 40"
        },
        "lastName": {
          ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 40"
        },
        "partnerID": {
          ".validate": "(newData.isString() && newData.val() != auth.uid) || !newData.exists()"
        },
        "publicKey": {
          ".validate": "newData.isString() && newData.val().length > 40"
        },
        "moodCiphertext": {
          ".validate": "newData.isString() && newData.val().length > 0"
        },
        "locationCiphertext": {
          ".validate": "newData.isString() && newData.val().length > 0"
        },
        "fcmToken": {
          ".validate": "newData.isString() && newData.val().length > 0"
        },
        "$other": {
          ".validate": false
        }
      }
    },
    "pairCodes": {
      "$code": {
        ".read": "auth != null",
        ".write": "auth != null && (!data.exists() || data.val() === auth.uid) && newData.exists() && newData.isString() && newData.val() === auth.uid",
        ".validate": "$code.matches(/^[0-9]{6}$/) && newData.isString() && newData.val() === auth.uid"
      }
    },
    "sessionLocks": {
      ".read": false,
      ".write": false
    },
    "chats": {
      "$chatId": {
        ".read": "auth != null && root.child('users').child(auth.uid).child('partnerID').val() != null && root.child('users').child(root.child('users').child(auth.uid).child('partnerID').val()).child('partnerID').val() === auth.uid && ($chatId === auth.uid + '_' + root.child('users').child(auth.uid).child('partnerID').val() || $chatId === root.child('users').child(auth.uid).child('partnerID').val() + '_' + auth.uid)",
        ".write": "auth != null && root.child('users').child(auth.uid).child('partnerID').val() != null && root.child('users').child(root.child('users').child(auth.uid).child('partnerID').val()).child('partnerID').val() === auth.uid && ($chatId === auth.uid + '_' + root.child('users').child(auth.uid).child('partnerID').val() || $chatId === root.child('users').child(auth.uid).child('partnerID').val() + '_' + auth.uid)",
        "messages": {
          ".indexOn": [
            "timestamp",
            "sentAt"
          ],
          "$messageId": {
            ".validate": "newData.hasChildren(['id','senderID','recipientID','payload','sentAt','keyVersion']) && newData.child('id').val() === $messageId && newData.child('senderID').val() === auth.uid && newData.child('recipientID').val() === root.child('users').child(auth.uid).child('partnerID').val() && newData.child('payload').isString() && newData.child('payload').val().length > 0 && newData.child('sentAt').isNumber() && newData.child('keyVersion').isNumber() && (!newData.child('timestamp').exists() || newData.child('timestamp').isNumber())",
            "id": {
              ".validate": "newData.isString()"
            },
            "senderID": {
              ".validate": "newData.isString()"
            },
            "recipientID": {
              ".validate": "newData.isString()"
            },
            "payload": {
              ".validate": "newData.isString() && newData.val().length > 0"
            },
            "sentAt": {
              ".validate": "newData.isNumber()"
            },
            "timestamp": {
              ".validate": "newData.isNumber()"
            },
            "keyVersion": {
              ".validate": "newData.isNumber()"
            },
            "$other": {
              ".validate": false
            }
          }
        }
      }
    },
    "events": {
      "$chatId": {
        ".read": "auth != null && root.child('users').child(auth.uid).child('partnerID').val() != null && root.child('users').child(root.child('users').child(auth.uid).child('partnerID').val()).child('partnerID').val() === auth.uid && ($chatId === auth.uid + '_' + root.child('users').child(auth.uid).child('partnerID').val() || $chatId === root.child('users').child(auth.uid).child('partnerID').val() + '_' + auth.uid)",
        ".write": "auth != null && root.child('users').child(auth.uid).child('partnerID').val() != null && root.child('users').child(root.child('users').child(auth.uid).child('partnerID').val()).child('partnerID').val() === auth.uid && ($chatId === auth.uid + '_' + root.child('users').child(auth.uid).child('partnerID').val() || $chatId === root.child('users').child(auth.uid).child('partnerID').val() + '_' + auth.uid)",
        "heartbeat": {
          "$eventId": {
            ".validate": "newData.hasChildren(['senderID','sentAt']) && newData.child('senderID').val() === auth.uid && newData.child('sentAt').isNumber()",
            "senderID": {
              ".validate": "newData.isString()"
            },
            "sentAt": {
              ".validate": "newData.isNumber()"
            },
            "$other": {
              ".validate": false
            }
          }
        },
        "messageReceipts": {
          ".read": "auth != null && root.child('users').child(auth.uid).child('partnerID').val() != null && root.child('users').child(root.child('users').child(auth.uid).child('partnerID').val()).child('partnerID').val() === auth.uid && ($chatId === auth.uid + '_' + root.child('users').child(auth.uid).child('partnerID').val() || $chatId === root.child('users').child(auth.uid).child('partnerID').val() + '_' + auth.uid)",
          ".write": false,
          ".indexOn": [
            "updatedAt"
          ]
        },
        "messageReactions": {
          ".read": "auth != null && root.child('users').child(auth.uid).child('partnerID').val() != null && root.child('users').child(root.child('users').child(auth.uid).child('partnerID').val()).child('partnerID').val() === auth.uid && ($chatId === auth.uid + '_' + root.child('users').child(auth.uid).child('partnerID').val() || $chatId === root.child('users').child(auth.uid).child('partnerID').val() + '_' + auth.uid)",
          ".write": false,
          ".indexOn": [
            "updatedAt"
          ]
        }
      }
    },
    "relationshipRequests": {
      ".indexOn": [
        "fromUID",
        "toUID",
        "status",
        "createdAt",
        "expiresAt"
      ],
      ".read": "auth != null && ((query.orderByChild == 'toUID' && query.equalTo == auth.uid) || (query.orderByChild == 'fromUID' && query.equalTo == auth.uid))",
      ".write": false,
      "$requestId": {
        ".read": "auth != null && (data.child('fromUID').val() === auth.uid || data.child('toUID').val() === auth.uid)",
        ".write": false
      }
    },
    "systemNotices": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid && !newData.exists()",
        ".validate": "!newData.exists() || (newData.hasChildren(['type','sourceUID','sourceName','createdAt']) && newData.child('type').isString() && newData.child('type').val() === 'partner_account_deleted' && newData.child('sourceUID').isString() && newData.child('sourceUID').val().length > 0 && newData.child('sourceName').isString() && newData.child('sourceName').val().length > 0 && newData.child('createdAt').isNumber())",
        "type": {
          ".validate": "newData.isString() && newData.val() === 'partner_account_deleted'"
        },
        "sourceUID": {
          ".validate": "newData.isString() && newData.val().length > 0"
        },
        "sourceName": {
          ".validate": "newData.isString() && newData.val().length > 0"
        },
        "createdAt": {
          ".validate": "newData.isNumber()"
        },
        "$other": {
          ".validate": false
        }
      }
    }
  }
}
